# 客户版考试平台 TODO

更新：2026-09-26。只针对 `client`，不要和自用版 `main` 的迁移链混在一起。

## 已交付

对照最初那句目标，这些已经有了：

- 老师上传试卷，AI 拆题，人工校对后发布给班级或指定学生
- 截止时间到点收卷；另外可以填开考后限时，两者都填时先到先收卷
- 强制全屏，退出、切页、粘贴会留下记录
- 客观题自动判分
- 主观题 AI 只出建议分和理由，老师采纳并保存后才进成绩；发布时可选宽松 / 标准 / 严格
- 班级考试概况、试卷分析，都有 ECharts
- 某一次作答能看每题停留多久；班级看板能打开某个学生的历次考试
- 主观题保留新增和删除的文字，能区分手敲、输入法、非键盘插入
- 学生交卷后可以逐题看自己的答案、得分和参考答案。待批改时只显示客观题小计，不给 AI 建议
- 监考中心可导出成绩单 PDF。只含教师已保存的分数；未批完的主观题留空，不记 0 分
- 管理员继续用原来的用户和数据管理，并在模型通道里配置「主观题 AI 批阅」

数据库迁移是 `20260925_0008`。限时写在发布对象里，没有单独再开迁移。导出成绩单也不需要新迁移。

## 还没做

这些不是已经答应的范围，按需要再做：

1. 用真实的「主观题 AI 批阅」通道走通一道简答题，确认建议分、失败原因、采纳后成绩都对。
2. 发布之后仍能改批阅尺度。只影响之后新的预批，不改已保存的成绩。
3. 行为记录分页。按题画像继续用聚合数字；展开新增/删除内容时按题分页拉 `answer_edit`，不要一次把整份事件拉进抽屉。
4. 一份卷主观题经常超过 5 道时，再把 AI 预批改成后台任务加进度。题量不大就保持现在这条同步请求。

## 不要做

- 不要让模型直接写正式成绩
- 不要用 0 分表示「没设分值」或「模型没返回分数」
- 不要把这些改动合并进 `main`，除非单独处理迁移
